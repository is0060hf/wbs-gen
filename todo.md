# WBS管理システム TODO

## 実装完了済み機能（Phase 3+ 追加実装まで）

### 基本機能（1-4）
- ✅ プロジェクト管理（新規作成、編集、サンプル生成）
- ✅ ファイル操作（JSON/テキストインポート、エクスポート、クリップボードコピー、ローディング表示付き）
- ✅ タスク管理（CRUD、階層構造、一括操作）
- ✅ タスク編集（インライン編集、詳細モーダル、全属性対応）

### 高度な機能（5-7）
- ✅ 依存関係管理（前提タスク設定・削除、循環依存検出、視覚的表示）
- ✅ スケジュール管理（クリティカルパス識別・ハイライト表示）
- ✅ 階層構造操作（展開/折りたたみ、全て展開/折りたたみ、WBSコード自動採番）

### ビュー機能（8-10）
- ✅ ガントチャートビュー（依存関係線表示、クリティカルパスハイライト）
- ✅ テーブルビュー（基本機能）
- ✅ フィルタリング・検索（フィルター条件保存・読み込み含む全機能）

### ユーザビリティ機能（11-13）
- ✅ ショートカットキー（Undo/Redo含む）
- ✅ 操作履歴（Undo/Redo、履歴表示、クリア）
- ✅ UI/UX機能（ダークモード、トースト通知、ローディング表示、プログレスバー、アニメーション）

### 今回の追加実装で完了した機能
- ✅ ガントチャートのタスクバークリックで編集モーダル表示
- ✅ 親タスクの開始日・期間・進捗率の自動計算（子タスクから集計）
- ✅ 親タスクの編集制限（編集モーダルで開始日・期間・進捗率を読み取り専用）
- ✅ ガントチャートでの親タスクのドラッグ無効化
- ✅ ドラッグによる値変更時のモーダル表示防止（実際に値が変更された場合のみ）

## Claudeレビューで指摘された改善点

### バグ修正
- ✅ TaskList.tsx内で `isSelected` の判定を修正（selectedTaskIds.includes(task.id)を使用）
- ✅ WBSContext.tsx の `ADD_SIBLING_TASK` でWBSコードの再計算を追加

### 可読性の改善
- ✅ TaskItem コンポーネントを分割（TaskActionsコンポーネントに分離）
- ✅ 長い三項演算子をヘルパー関数に置き換え（getPriorityClass関数など）

### 追加対応完了
- ✅ getPriorityClass関数をtask-utils.tsに移動して再利用可能に
- ✅ TaskActionsコンポーネントにメニュー外クリックで閉じる処理を追加
- ✅ 子タスクの選択状態と展開状態の正しい管理を実装

注：Claudeレビューで以下の推奨事項（必須ではない）の指摘があったが、今回は対応を見送り
- recalculateWBSCodes関数に単体テストを追加
- recalculateWBSCodes関数をwbs-utils.tsに移動してテスタビリティを向上
- getPriorityClass関数のユニットテストを追加
（レビュー総評：「全体的に良好な実装。主要なバグは修正済み、現状でも十分に機能します」）

## Claudeレビューで推奨された改善項目（ガントチャート機能追加後）

### 将来の改善項目（必須ではない推奨事項）
- [ ] .ideaファイルをgitignoreに追加（IDEの設定ファイル除外）
- [ ] DOM要素のnullチェック強化（barElementのinstanceof HTMLElementチェック）
- [ ] GanttChart.tsxコンポーネントの分割（700行以上のため）
  - ドラッグ&ドロップロジックをuseGanttDragDropカスタムフックに抽出
  - ズームコントロールを別コンポーネントに分離
  - タスクバーの描画ロジックを別コンポーネントに分離
- [ ] マジックナンバーの定数化（ZOOM_SCALE_FACTOR, MAX_ZOOM_LEVEL等）
- [ ] ガントチャート機能のユニットテスト追加（日付計算、ドラッグ&ドロップ、ズーム機能）
（レビュー総評：「機能は正しく実装されており、セキュリティ上の問題もない。保守性とテスト可能性向上のためのリファクタリングを推奨」）

## Claudeレビューで推奨された改善項目（ビューモード対応修正後）

### 将来の改善項目（必須ではない推奨事項）
- [ ] DOM操作の直接使用を避け、Reactのstateで管理（ドラッグ中のタスクバー位置）
- [ ] generateDependencyPath内でのgetTaskBarStyleの重複計算をメモ化
- [ ] Date操作のエラーハンドリング強化（無効な日付の検証）
（レビュー総評：「コードは全体的によく構造化されており、TypeScriptの型も適切に使用されています」）

## Claudeレビューで推奨された改善項目（日表示限定実装後）

### 将来の改善項目（必須ではない推奨事項）
- [ ] CHART_CONSTANTSの使用確認（未使用なら削除可能）
- [ ] handleReset関数をresetToTodayなどの明確な名前に変更
（レビュー総評：「日付計算のロジックが簡素化され、バグのリスクが減少」）

## Claudeレビューで推奨された改善項目（親タスク自動計算実装後）

### 将来の改善項目（必須ではない推奨事項）
- [ ] 期間計算のバグ修正（0.5日単位対応）
- [ ] 親タスク計算ロジックのユニットテスト追加
（レビュー総評：「TypeScriptの型は適切に使用されています。型安全性に問題はありません」）

## Claudeレビューで推奨された改善項目（ドラッグ&ドロップ機能実装後）

### 将来の改善項目（必須ではない推奨事項）
- [ ] isDescendantOf関数のユニットテスト追加
（レビュー総評：「ドラッグ&ドロップ機能は正しく実装されており、主要なエッジケースの処理も適切」）

## 残りの実装予定（Phase 4以降）

### 今回の追加実装で完了した機能
- ✅ クリティカルパスのハイライト表示（ガントチャート）
- ✅ 全て展開/全て折りたたみ機能（タスクリスト）
- ✅ フィルター条件の保存・読み込み機能
- ✅ ローディング表示（インポート/エクスポート時）
- ✅ プログレスバー表示コンポーネント

### 高度なガントチャート機能
- ✅ 依存関係線の表示
- ✅ クリティカルパスのハイライト表示
- ✅ タスクバーのドラッグによる日程変更
- ✅ ズームイン/ズームアウト（※日表示のみに限定実装）

### ドラッグ&ドロップ機能
- ✅ ドラッグ&ドロップ基盤（Context/Hook実装済み、実際の使用は次のフェーズ）
- ✅ タスクの並び替え（ドラッグ&ドロップ）
- ✅ 階層変更（ドラッグ&ドロップ）

### テーブルビュー強化
- ✅ カラムの表示/非表示切替
- ✅ カラム幅の調整
- ✅ カラムの並び替え
- ✅ 各カラムでのソート（昇順/降順）
- ✅ ヘッダー固定スクロール

### パフォーマンス最適化
- [ ] 仮想スクロール（大量タスク対応）
- [ ] メモ化による再レンダリング防止
- [ ] デバウンス処理強化

### その他の高度な機能
- [ ] 印刷用レイアウト
- [ ] エクスポート機能拡張（Excel、PDF）
- [ ] データの自動保存
- [ ] バックアップ機能
- [ ] アクセシビリティの向上（WCAG 2.2準拠）
